name: Build Linux and Android

on:
  push:
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  CARGO_NET_RETRY: "10"
  DEBIAN_FRONTEND: noninteractive

jobs:
  linux-appimage:
    name: Linux AppImage (${{ matrix.artifact_arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            artifact_arch: x86_64
            rustup_arch: x86_64
            linuxdeploy_arch: x86_64
            linuxdeploy_sha256: c20cd71e3a4e3b80c3483cef793cda3f4e990aca14014d23c544ca3ce1270b4d
            artifact_name: calibraw-linux-appimage
          - runner: ubuntu-24.04-arm
            artifact_arch: arm64
            rustup_arch: aarch64
            linuxdeploy_arch: aarch64
            linuxdeploy_sha256: 620095110d693282b8ebeb244a95b5e911cf8f65f76c88b4b47d16ae6346fcff
            artifact_name: calibraw-linux-appimage-arm64
    env:
      CALIBRAW_REQUIRE_LENSFUN: "1"
      LIBRAW_PREFIX: /tmp/calibraw-libraw
      PKG_CONFIG_PATH: /tmp/calibraw-libraw/lib/pkgconfig
      LD_LIBRARY_PATH: /tmp/calibraw-libraw/lib

    steps:
      - name: Check out repository
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          fetch-depth: 0

      - name: Restore Linux build cache
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
            /tmp/calibraw-libraw
            /tmp/linuxdeploy-${{ matrix.linuxdeploy_arch }}.AppImage
          key: calibraw-linux-native-v2-${{ runner.os }}-${{ runner.arch }}-rust-1.92.0-libraw-823f25d-${{ hashFiles('Cargo.lock', 'Cargo.toml', 'crates/**/Cargo.toml', 'crates/**/build.rs', 'crates/**/build_support/**') }}
          restore-keys: |
            calibraw-linux-native-v2-${{ runner.os }}-${{ runner.arch }}-rust-1.92.0-libraw-823f25d-

      - name: Install Linux build dependencies
        shell: bash
        run: |
          set -euxo pipefail

          if [ "$(id -u)" -eq 0 ]; then
            SUDO=""
          else
            SUDO="sudo"
          fi

          $SUDO apt-get update
          $SUDO apt-get install -y --no-install-recommends \
            build-essential \
            autoconf \
            automake \
            ca-certificates \
            clang \
            curl \
            file \
            libasound2-dev \
            libclang-dev \
            libdbus-1-dev \
            libegl1-mesa-dev \
            libfontconfig1-dev \
            libglib2.0-dev \
            libgl1-mesa-dev \
            libjpeg-dev \
            liblensfun-dev \
            liblensfun-data-v1 \
            libtool \
            libudev-dev \
            libwayland-dev \
            libx11-dev \
            libxkbcommon-dev \
            patchelf \
            pkg-config \
            python3 \
            squashfs-tools \
            wget \
            zlib1g-dev

      - name: Build release binary
        env:
          CALIBRAW_DISCORD_APPLICATION_ID: ${{ secrets.CALIBRAW_DISCORD_APPLICATION_ID }}
          RUSTUP_ARCH: ${{ matrix.rustup_arch }}
        shell: bash
        run: bash scripts/build_linux_release.sh

      - name: Package AppImage
        env:
          LINUXDEPLOY_ARCH: ${{ matrix.linuxdeploy_arch }}
          LINUXDEPLOY_SHA256: ${{ matrix.linuxdeploy_sha256 }}
        shell: bash
        run: bash scripts/package_linux_appimage.sh

      - name: Upload AppImage
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/*
          if-no-files-found: error

  android-apk:
    name: Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          fetch-depth: 0

      - name: Restore pinned Android toolchains
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5.1.0
        with:
          path: |
            ~/android-sdk
            ~/.gradle/wrapper
            ~/.cargo/bin/cargo-ndk
            ~/.cargo/bin/cargo-ndk-env
            ~/.cargo/bin/cargo-ndk-runner
            ~/.cargo/bin/cargo-ndk-test
          key: calibraw-android-tools-${{ runner.os }}-${{ runner.arch }}-arm64-${{ hashFiles('Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'gradle/wrapper/gradle-wrapper.properties', 'scripts/bootstrap_download.py', 'xtask/src/main.rs') }}-v2

      - name: Restore Android dependency and build cache
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
            ~/.gradle/caches
            ~/.gradle/notifications
            android/.gradle
          key: calibraw-android-build-${{ runner.os }}-${{ runner.arch }}-arm64-rust-1.92.0-${{ hashFiles('Cargo.lock', 'Cargo.toml', 'build.rs', 'android/build.gradle', 'android/app/build.gradle') }}
          restore-keys: |
            calibraw-android-build-${{ runner.os }}-${{ runner.arch }}-arm64-rust-1.92.0-

      - name: Restore Android native dependency cache
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5.1.0
        with:
          path: android/native
          key: android-native-deps-v1-${{ runner.os }}-${{ hashFiles('Cargo.toml', 'Cargo.lock', 'android/app/src/main/cpp/CMakeLists.txt', 'xtask/src/main.rs') }}
          restore-keys: |
            android-native-deps-v1-${{ runner.os }}-

      - name: Set up Java 17
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "17"

      - name: Install Android and native build dependencies
        shell: bash
        run: |
          set -euxo pipefail

          if [ "$(id -u)" -eq 0 ]; then
            SUDO=""
          else
            SUDO="sudo"
          fi

          $SUDO apt-get update
          $SUDO apt-get install -y --no-install-recommends \
            build-essential \
            ca-certificates \
            clang \
            curl \
            libclang-dev \
            pkg-config \
            tar \
            unzip \
            zip

      - name: Prepare release signing keystore
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: bash
        env:
          KEYSTORE_BASE64: ${{ secrets.CALIBRAW_ANDROID_KEYSTORE_BASE64 }}
          CALIBRAW_ANDROID_STORE_PASSWORD: ${{ secrets.CALIBRAW_ANDROID_STORE_PASSWORD }}
          CALIBRAW_ANDROID_KEY_ALIAS: ${{ secrets.CALIBRAW_ANDROID_KEY_ALIAS }}
          CALIBRAW_ANDROID_KEY_PASSWORD: ${{ secrets.CALIBRAW_ANDROID_KEY_PASSWORD }}
        run: |
          set -euo pipefail

          : "${KEYSTORE_BASE64:?Missing CALIBRAW_ANDROID_KEYSTORE_BASE64 repository secret}"
          : "${CALIBRAW_ANDROID_STORE_PASSWORD:?Missing CALIBRAW_ANDROID_STORE_PASSWORD repository secret}"
          : "${CALIBRAW_ANDROID_KEY_ALIAS:?Missing CALIBRAW_ANDROID_KEY_ALIAS repository secret}"
          : "${CALIBRAW_ANDROID_KEY_PASSWORD:?Missing CALIBRAW_ANDROID_KEY_PASSWORD repository secret}"

          keystore_path="$RUNNER_TEMP/calibraw-release.keystore"
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > "$keystore_path"
          chmod 600 "$keystore_path"
          keytool -list \
            -keystore "$keystore_path" \
            -storepass:env CALIBRAW_ANDROID_STORE_PASSWORD \
            -alias "$CALIBRAW_ANDROID_KEY_ALIAS" >/dev/null
          {
            printf 'CALIBRAW_ANDROID_KEYSTORE_PATH=%s\n' "$keystore_path"
            printf 'CALIBRAW_ANDROID_STORE_PASSWORD=%s\n' "$CALIBRAW_ANDROID_STORE_PASSWORD"
            printf 'CALIBRAW_ANDROID_KEY_ALIAS=%s\n' "$CALIBRAW_ANDROID_KEY_ALIAS"
            printf 'CALIBRAW_ANDROID_KEY_PASSWORD=%s\n' "$CALIBRAW_ANDROID_KEY_PASSWORD"
          } >> "$GITHUB_ENV"

      - name: Build arm64 Android APK
        id: build_android_apk
        shell: bash
        run: |
          set -euxo pipefail

          bootstrap_download_verified() {
            python3 scripts/bootstrap_download.py "$1" "$2" "$3"
          }

          mkdir -p android/native/downloads
          if ! bootstrap_download_verified \
            https://github.com/neovim/deps/raw/b9bf36eb31f27e8136d907da38fa23518927737e/opt/libiconv-1.17.tar.gz \
            android/native/downloads/libiconv-1.17.tar.gz \
            8f74213b56238c85a50a5329f77e06198771e70dd9a739779f4c02f65d971313; then
            bootstrap_download_verified \
              https://mirrors.edge.kernel.org/gnu/libiconv/libiconv-1.17.tar.gz \
              android/native/downloads/libiconv-1.17.tar.gz \
              8f74213b56238c85a50a5329f77e06198771e70dd9a739779f4c02f65d971313
          fi

          bootstrap_download_verified \
            https://static.rust-lang.org/rustup/archive/1.28.2/x86_64-unknown-linux-gnu/rustup-init \
            /tmp/rustup-init \
            https://static.rust-lang.org/rustup/archive/1.28.2/x86_64-unknown-linux-gnu/rustup-init.sha256
          chmod +x /tmp/rustup-init
          /tmp/rustup-init -y --profile minimal --default-toolchain 1.92.0
          source "$HOME/.cargo/env"
          rustup target add aarch64-linux-android
          if [ "$(cargo ndk --version 2>/dev/null || true)" != "cargo-ndk 4.1.2" ]; then
            cargo install cargo-ndk --version 4.1.2 --locked
          fi

          build_metadata="$(cargo metadata --locked --no-deps --format-version 1)"
          export CALIBRAW_ANDROID_COMPILE_SDK="$(printf '%s' "$build_metadata" | python3 -c 'import json,sys; print(json.load(sys.stdin)["metadata"]["android_compile_sdk"])')"
          export CALIBRAW_ANDROID_BUILD_TOOLS_VERSION="$(printf '%s' "$build_metadata" | python3 -c 'import json,sys; print(json.load(sys.stdin)["metadata"]["android_build_tools_version"])')"
          export CALIBRAW_ANDROID_NDK_VERSION="$(printf '%s' "$build_metadata" | python3 -c 'import json,sys; print(json.load(sys.stdin)["metadata"]["android_ndk_version"])')"

          install_sdk_packages() {
            local attempt
            for attempt in 1 2 3 4; do
              if sdkmanager \
                "platform-tools" \
                "platforms;android-$CALIBRAW_ANDROID_COMPILE_SDK" \
                "build-tools;$CALIBRAW_ANDROID_BUILD_TOOLS_VERSION" \
                "ndk;$CALIBRAW_ANDROID_NDK_VERSION" \
                "cmake;$ANDROID_CMAKE_VERSION"; then
                return 0
              fi
              echo "sdkmanager failed on attempt $attempt; retrying..." >&2
              sleep $((attempt * 10))
            done
            return 1
          }

          ANDROID_HOME="$HOME/android-sdk"
          ANDROID_SDK_ROOT="$ANDROID_HOME"
          ANDROID_CMAKE_VERSION="3.22.1"
          export ANDROID_HOME ANDROID_SDK_ROOT ANDROID_CMAKE_VERSION

          if [ ! -x "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" ]; then
            rm -rf /tmp/android-command-line-tools
            mkdir -p "$ANDROID_HOME/cmdline-tools" /tmp/android-command-line-tools
            ANDROID_CMDLINE_TOOLS_SHA512=b65e830d7655fb39cc9eee669806977f462c49375807ef2c6487fabcc9afdbc210465ce6a1e2429ff95c74ca519d1239daf9a403c30b8d0bdb7a0962af656c8e
            bootstrap_download_verified \
              https://dl.google.com/android/repository/commandlinetools-linux-14742923_latest.zip \
              /tmp/android-command-line-tools.zip \
              "sha512:$ANDROID_CMDLINE_TOOLS_SHA512"
            unzip -q /tmp/android-command-line-tools.zip -d /tmp/android-command-line-tools
            rm -rf "$ANDROID_HOME/cmdline-tools/latest"
            mv /tmp/android-command-line-tools/cmdline-tools \
              "$ANDROID_HOME/cmdline-tools/latest"
          fi

          export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH"
          yes | sdkmanager --licenses >/dev/null || true
          install_sdk_packages
          export ANDROID_NDK_HOME="$ANDROID_HOME/ndk/$CALIBRAW_ANDROID_NDK_VERSION"
          export PATH="$ANDROID_HOME/cmake/$ANDROID_CMAKE_VERSION/bin:$PATH"

          LIBCLANG_SO="$(find /usr/lib -path '*/llvm-*/lib/libclang.so*' -print -quit 2>/dev/null || true)"
          if [ -n "$LIBCLANG_SO" ]; then
            export LIBCLANG_PATH="$(dirname "$LIBCLANG_SO")"
          fi

          bash scripts/generate_licenses.sh
          CALIBRAW_ALLOW_NO_LIBRAW=1 cargo check --locked --workspace
          ./gradlew --version

          if [ "$GITHUB_EVENT_NAME" = "push" ] && [ "$GITHUB_REF" = "refs/heads/main" ]; then
            gradle_task=assembleRelease
            apk_path="android/app/build/outputs/apk/release/app-release.apk"
            output_name="CalibRaw-release-arm64-v8a.apk"
            artifact_name="calibraw-android-release-arm64-v8a"
            signing_args=(-PcalibrawRequireReleaseSigning=true)
          else
            gradle_task=assembleDebug
            apk_path="android/app/build/outputs/apk/debug/app-debug.apk"
            output_name="CalibRaw-debug-arm64-v8a.apk"
            artifact_name="calibraw-android-debug-arm64-v8a"
            signing_args=()
          fi

          ./gradlew \
            --no-daemon \
            --stacktrace \
            -p android \
            "$gradle_task" \
            -PcalibrawAbis=arm64-v8a \
            -PcalibrawVersionCode="$GITHUB_RUN_NUMBER" \
            "${signing_args[@]}"

          APK="$apk_path"
          test -f "$APK"
          cargo xtask verify-android-16kb "$APK"
          if [ "$GITHUB_EVENT_NAME" = "push" ] && [ "$GITHUB_REF" = "refs/heads/main" ]; then
            "$ANDROID_HOME/build-tools/$CALIBRAW_ANDROID_BUILD_TOOLS_VERSION/apksigner" \
              verify --verbose --print-certs "$APK"
          fi
          mkdir -p dist
          cp "$APK" "dist/$output_name"
          sha256sum "dist/$output_name" > "dist/$output_name.sha256"
          echo "artifact_name=$artifact_name" >> "$GITHUB_OUTPUT"

      - name: Remove release signing keystore
        if: always() && github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: bash
        run: |
          rm -f "$RUNNER_TEMP/calibraw-release.keystore"
          {
            echo "CALIBRAW_ANDROID_KEYSTORE_PATH="
            echo "CALIBRAW_ANDROID_STORE_PASSWORD="
            echo "CALIBRAW_ANDROID_KEY_ALIAS="
            echo "CALIBRAW_ANDROID_KEY_PASSWORD="
          } >> "$GITHUB_ENV"

      - name: Upload Android APK
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ steps.build_android_apk.outputs.artifact_name }}
          path: dist/*
          if-no-files-found: error
