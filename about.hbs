# Rust dependency licenses

This file is generated from `Cargo.lock` by cargo-about. It covers the resolved
Rust dependencies used by CalibRaw's supported release targets. Release packaging regenerates it with cargo-about 0.9.2 via:

`bash scripts/prepare_release_licenses.sh`

{{#each licenses}}
## {{{name}}} (`{{id}}`)

Used by:
{{#each used_by}}
- `{{crate.name}} {{crate.version}}`{{#if crate.repository}} — {{{crate.repository}}}{{/if}}
{{/each}}

```text
{{{text}}}
```

{{/each}}
